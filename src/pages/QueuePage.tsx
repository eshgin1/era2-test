import { QueueProvider } from '@/features/generation-queue/model/QueueProvider';
import { GenerationQueue } from '@/widgets/generation-queue/ui/GenerationQueue';

export default function QueuePage() {
  return (
    <GenerationQueue />
  );
}